services:
  bienestar:
    build: .
    container_name: bienestar_app
    ports:
      - "8501:8501"
    volumes:
      - ./app/data:/app/data  # persist SQLite DB
    environment:
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false